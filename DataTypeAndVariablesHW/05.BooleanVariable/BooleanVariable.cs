using System;
class BooleanVariable
{
    // Declare a Boolean variable called isFemale and assign an appropriate value corresponding to
    // your gender.Print it on the console.
    static void Main()
    {
        bool isFemale = false;
        Console.WriteLine("Is my gender Female? - " + isFemale);
    }
}
